# parse_spc accounts for uppercase arguments and spec

    Code
      parse_spc(text)
    Output
      series{
        title = "xyz"
        start = 2014.01
        period = 12
        save = (A1 B1)
        print = BRIEF
        name = "abc"
        file = "path/to/file.dat"
      } 

